import numpy as np
import tensor as tf  ### error  wrong typing                                    
import sklearn
import cv2

quote = (12*("*")+ ("\n")  + "All it's ok \n"+ 12*("*")+("\n"))

with open('conda.out', 'w', encoding='utf-8') as f:
    f.write(quote)
